diff --git a/misaka/api.py b/misaka/api.py
index aff8f82..d966204 100644
--- a/misaka/api.py
+++ b/misaka/api.py
@@ -306,7 +306,7 @@ class SaferHtmlRenderer(HtmlRenderer):
         Filters links generated by the ``autolink`` extension.
         """
         if self.check_url(raw_url):
-            url = self.rewrite_url(('mailto:' if is_email else '') + raw_url)
+            url = self.rewrite_url(('mailto:' if not is_email else '') + raw_url)
             url = escape_html(url)
             return '<a href="%s">%s</a>' % (url, escape_html(raw_url))
         else:
